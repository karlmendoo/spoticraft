package io.github.karlmendoo.spoticraft;

/**
 * Shared (common) entrypoint placeholder.
 * SpotiCraft is a client-only mod, so the actual logic is in {@code SpotiCraftClient}.
 */
public class SpotiCraft {
    public static final String MOD_ID = "spoticraft";
}
